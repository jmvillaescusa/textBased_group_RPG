#include <iostream>
#include <string>

using namespace std;


int main() {

	const int MAX_ITEMS = 7;
	string inventory[MAX_ITEMS];

	int numItems = 0;
	inventory[numItems++] = "Rations";
	inventory[numItems++] = "Light Shiv";
	inventory[numItems++] = "Ragged Clothes";
	inventory[numItems++] = "Pew pew rifle";



	return 0;
}

